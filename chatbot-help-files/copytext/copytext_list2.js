function copyTextA() {
    const textElement = document.getElementById('text-to-copy-A');
    copyToClipboard(textElement.textContent, 'Primeiro texto copiado para a área de transferência!');
}

function copyTextB() {
    const textElement = document.getElementById('text-to-copy-B');
    copyToClipboard(textElement.textContent, 'Primeiro texto copiado para a área de transferência!');
}

function copyTextC() {
    const textElement = document.getElementById('text-to-copy-C');
    copyToClipboard(textElement.textContent, 'Primeiro texto copiado para a área de transferência!');
}

function copyTextD() {
    const textElement = document.getElementById('text-to-copy-D');
    copyToClipboard(textElement.textContent, 'Primeiro texto copiado para a área de transferência!');
}